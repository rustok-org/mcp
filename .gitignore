# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Virtual environments
.venv
venv/
ENV/
env/

# IDEs
.idea/
.vscode/
*.swp
*.swo
*~

# Testing
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# Rust (legacy — kept for safety during transition)
debug/
target/
**/*.rs.bk
*.pdb

# Secrets — never commit real env files; the example is tracked.
.env
.env.*
!.env.example

# Workflow artifacts — the whole directory, not selected parts of it.
# These are internal working notes: specs, decisions, reports, the state file.
# They are written for us, not for a reader of this repository, and they are
# kept outside the published tree.
.claude/
.review.diff

# Misc
.DS_Store
