# ТЗ: ответ на аудит ClawHub (Critical-ложняк + формулировка раскрытия)

> Статус: **план Ревьюера, ждёт «го» Капитана.** Кода нет.
> Повод: аудит ClawHub скилла `rustok-wallet` 0.4.9 (2026-08-02 19:34 UTC) —
> Critical «Exposed hardcoded secret» на `SKILL.md:54` + Overview, пересказывающий
> наше собственное раскрытие про `sign_typed_data`.
> Место в очереди: микрокруг **после** консольной утечки пароля, **до/параллельно** update-check.

## Разбор находок (проверено Ревьюером 2026-08-03)

**Находка 1 — ЛОЖНАЯ.** `SKILL.md:54`:
`read -r -s -p "Keyring password: " pw && printf '%s' "$pw" | podman secret create rustok-keyring - && unset pw`
Литерала секрета нет: интерактивный ввод без эха → секрет-хранилище podman → `unset`.
Гипотеза триггера (НЕ проверена): наивное правило вида «ключевое слово `password`/`secret`
рядом с литералом в одной строке». Страница аудита не показывает ни совпавшую подстроку,
ни путь оспаривания — искали специально.

**Находка 2 — НЕ находка.** Overview («alternate EIP-712 endpoint that can bypass the
advertised restricted-agent controls») — пересказ нашего раскрытия `SKILL.md:187-193`,
вписанного осознанно в 0.4.8 по ADR «вариант A». Факт верен. Опущен материальный контекст,
который в коде ЕСТЬ и проверен: шлюз по умолчанию только loopback
(`src/rustok_mcp/config.py:26` — `host: 127.0.0.1`; entrypoint поднимает core/gateway на
`127.0.0.1`).

## Шаг 1 — измерить, а не гадать (обязателен ПЕРВЫМ)

Локально ни одного сканера секретов нет (проверено: gitleaks/trufflehog/detect-secrets/
semgrep/ripsecrets — отсутствуют). Поставить один (кандидат: gitleaks) и **воспроизвести
находку на текущем `SKILL.md`**. Дальше двигаться только по факту совпадения:
- сработало → есть измеримая красная фаза, правку проверяем ею же;
- не сработало → правило ClawHub своё, и единственный честный путь — оспаривание;
  косметику **не делаем вслепую**.

## Шаг 2 — правка формулировки (ТОЛЬКО безвредная)

**Разрешено:** разбить однострочник на три строки (читается лучше, поведение байт-в-байт то же):
```bash
read -r -s -p "Keyring password: " pw
printf '%s' "$pw" | podman secret create rustok-keyring -
unset pw
```
и/или переименовать секрет `rustok-keyring` → нейтральное имя.

**ЗАПРЕЩЕНО** (это была бы подгонка под гейт, правило №10): ослаблять сам рецепт, убирать
`read -s`, прятать рецепт из скилла, удалять раскрытие про `sign_typed_data`.
Граница: меняем ФОРМУ подачи, не МЕНЯЕМ смысл и ничего не скрываем.

## Шаг 3 — дополнить раскрытие, не смягчая

К `SKILL.md:187-193` добавить материальный контекст, которого там нет (он сейчас только во
frontmatter): эндпоинт по умолчанию слушает loopback; чтобы им воспользоваться, нужно уже
быть на машине пользователя; сетевая экспозиция — явный opt-in.

**Принцип формулировки риска (для всех будущих текстов):** риск → область действия →
что делать. «Не гейтится capabilities» (риск) + «доступен только с loopback, пока вы сами
не открыли наружу» (область) + «относитесь к вызывающему как к `execute_send`» (действие).
Первую часть НИКОГДА не выбрасываем — иначе это уже обман, а не формулировка.

## Шаг 4 — системный фикс (главное; класс, а не экземпляр)

Добавить в CI репо `mcp` джобу скана секретов по `skills/` + `docs/` **до** заливки на
ClawHub. Сегодняшний урок замороженной линии повторяется один в один: проверки не было →
проблема всплыла публично. Пусть красный бейдж появляется у нас в PR, а не на витрине.

## Шаг 5 — ops Капитана (веб)

Оспорить находку 1 на ClawHub: строка не содержит литерала секрета, это интерактивный ввод
+ секрет-хранилище + очистка; просить показать совпавший паттерн. Заодно выяснить, что
означает статус `Review` для установки скилла и есть ли механизм ответа мейнтейнера.

## Проверить отдельно (не забыть)

Финальная точка круга 0.4.3 не подтверждена живьём: `create-wallet` на опубликованном
`ghcr.io/rustok-org/rustok-wallet:latest` (дайджест `d120b41d2617`) печатает 12 слов.
